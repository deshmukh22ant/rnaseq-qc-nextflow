params {
    input  = null
    outdir = null
}

process {
    memory = '3 GB'
    cpus   = 1
}

executor {
    queueSize = 2
}

profiles {
    docker {
        docker.enabled = true
    }
    local {
        docker.enabled = false
    }
}

report.enabled   = true
timeline.enabled = true